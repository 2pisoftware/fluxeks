{
    "name": "Fedora",
    "build": {
        "dockerfile": "dockerfile",
        "context": "."
    },
    "customizations": {
        "vscode": {
            "settings": { 
                "workbench.colorTheme": "Default Dark+",
                //zsh
                "terminal.integrated.shell.linux": "/bin/zsh"
            }
        }
    }
}
